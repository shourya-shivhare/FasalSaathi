# FastAPI + Python Backend Dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
pydantic[email]>=2.7.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9   # Required for UploadFile / multipart form-data

# HTTP Client (for AI service proxy)
httpx>=0.27.0

# Env
python-dotenv>=1.0.0

# YOLO inference (shared via ai-service; install here too for detect endpoint)
ultralytics>=8.2.0
opencv-python>=4.9.0
